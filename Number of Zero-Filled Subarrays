long long cnt=0,res=0;
         for(int i=0;i<n;i++)
         {
             if(nums[i]==0)
                 cnt++;
             else
                 cnt=0;
             res+=cnt;
             cout<<"res now is"<<res<<endl;
         }
         return res;
        
